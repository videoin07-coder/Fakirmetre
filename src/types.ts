export type AppState = 'splash' | 'onboarding' | 'locked' | 'app';
export type ThemeMode = 'dark' | 'light';
export type TabKey = 'home' | 'quiz' | 'astro' | 'kitchen' | 'topluluk' | 'settings';
export type ZodiacKey = 'Koç'|'Boğa'|'İkizler'|'Yengeç'|'Aslan'|'Başak'|'Terazi'|'Akrep'|'Yay'|'Oğlak'|'Kova'|'Balık';
export type QuizMode = 'fakir'|'tasarruf'|'harcama'|'risk';
export type AccentKey = 'blue'|'purple'|'green'|'orange'|'pink';
export type MealEntry = { id:string; name:string; calories:number; time:string };
export type ExpenseEntry = { id:string; title:string; amount:number; category:string; createdAt:string };
export type SavingsGoal = { id:string; title:string; target:number; current:number; createdAt:string };
export type HistoryItem = { id:string; score:number; maxScore:number; title:string; date:string; level:string; emoji:string; quizMode:QuizMode; xpEarned:number };
export type Profile = { name:string; avatar:string; bio:string; xp:number; level:number; streak:number; lastLoginDate:string; totalTests:number; bestScore:number; zodiac:ZodiacKey; birthYear:string };
export type SettingsState = { darkMode:boolean; starsEnabled:boolean; showQuote:boolean; showAstrology:boolean; showChallenges:boolean; showInsights:boolean; accent:AccentKey; pinEnabled:boolean; pinCode:string; shareSignature:boolean; };
export type PremiumTier = 'free'|'plus'|'pro';
export type AiMessage = { role:'user'|'assistant'; content:string };
export type NotificationItem = { id:string; title:string; body:string; time:string; read:boolean; icon:string };
export type SeasonChallenge = { id:string; title:string; desc:string; xp:number; done:boolean; icon:string; target:number; current:number };
export type LeaderboardEntry = { name:string; avatar:string; xp:number; level:number };
export type HelpRequest = { id:string; avatar:string; name:string; title:string; desc:string; category:string; time:string; likes:number; replies:number; solved:boolean; level:number };
export type CommunityPost = { id:string; avatar:string; name:string; content:string; likes:number; time:string; tag:string; likedByMe:boolean };
export type BackupSnapshot = { version:string; exportedAt:string; profile:Profile; history:HistoryItem[]; settings:SettingsState; meals?:MealEntry[]; expenses?:ExpenseEntry[]; savingsGoals?:SavingsGoal[]; meta:{ source:'local-cloud'|'manual-export'|'manual-import'; app:string } };
